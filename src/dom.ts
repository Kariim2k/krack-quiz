export const questionElement = document.getElementById(
  "question",
) as HTMLHeadingElement;

export const optionsElement = document.getElementById(
  "options",
) as HTMLDivElement;

export const scoreElement = document.getElementById("score") as HTMLSpanElement;

export const nextBtn = document.getElementById("nextBtn") as HTMLButtonElement;

export const progressElement = document.getElementById(
  "progress",
) as HTMLParagraphElement;

export const timerElement = document.getElementById("timer") as HTMLSpanElement;

export const restartBtn = document.getElementById(
  "restartBtn",
) as HTMLButtonElement;

export const progressBar = document.getElementById(
  "progressBar",
) as HTMLDivElement;

export const logoutBtn = document.getElementById(
  "logoutBtn",
) as HTMLButtonElement;

export const showPasswordBtn = document.getElementById(
  "showPasswordBtn",
) as HTMLButtonElement;

export const authError = document.getElementById(
  "authError",
) as HTMLParagraphElement;
